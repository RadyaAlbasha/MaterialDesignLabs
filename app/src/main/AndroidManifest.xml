<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.materialdesign.labs.playwithlayouts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screens.registrationformscreen.RegistrationFormActivity"></activity>
        <activity android:name=".screens.coordinatorscreen.CoordinatorActivity" />
        <activity android:name=".screens.swipetabsscreen.SwipeTabsActivity" />
        <activity android:name=".screens.dayslistscreen.DaysListActivity" />
        <activity android:name=".screens.textdecorationscreen.TextDecorationActivity" />
        <activity android:name=".screens.colorsscreen.ColorsActivity" />
        <activity android:name=".screens.fourseasonsscreen.FourSeasonsActivity" />
        <activity android:name=".screens.mainscreen.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>